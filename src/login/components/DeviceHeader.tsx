import { h } from 'preact';

const DeviceHeader = (): JSX.Element => (
  <div class='code-header'>
    <div class='code-header__content'>
      <div class='code-header__title'>
        <span>Almost there!</span>
      </div>
    </div>
  </div>
);

export default DeviceHeader;
