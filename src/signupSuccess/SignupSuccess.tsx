import { h } from 'preact';

interface Props {
  path: string;
}

const SignupSuccess = (props: Props): JSX.Element => {
  return (
    <div>
      Success
    </div>
  );
};

export default SignupSuccess;
